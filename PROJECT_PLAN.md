# 多准则可持续披露报告 AI 能力包项目计划

## 1. 文档用途

本文档是项目管理和跨对话交接入口，用于记录当前状态、开发依赖、工作流、里程碑、责任人和下一步行动。

核心文档及职责如下：

- [REQUIREMENTS.md](./REQUIREMENTS.md)：业务需求和内部评审依据；
- [PRD.md](./PRD.md)：开发功能、数据契约、验收用例和完成标准；
- [HARNESS_ARCHITECTURE.md](./HARNESS_ARCHITECTURE.md)：Garden 风格 Skill Harness 架构和稳定性协议；
- `PROJECT_PLAN.md`：项目状态、任务顺序、依赖和交接说明。

文档优先级和约束关系如下：

1. `REQUIREMENTS.md` 定义业务意图、目标和范围；
2. `PRD.md` 将业务意图转化为外部行为、公共路径和公共数据契约；
3. `HARNESS_ARCHITECTURE.md` 只能在不改变 PRD 契约的前提下定义实现架构和稳定性协议；
4. `PROJECT_PLAN.md` 只管理进度、阶段范围、依赖和交接，不改变产品行为。

出现冲突时，较低层文档必须先向较高层契约对齐；若高层文档本身存在歧义，再暂停相关开发并由项目负责人确认。开发人员或 Agent 不得静默选择解释。

## 2. 当前状态

| 项目 | 当前情况 |
| --- | --- |
| 更新日期 | 2026-07-13 |
| 当前阶段 | M2 已完成，下一步启动 M3 |
| 需求状态 | 已形成业务需求文档和开发版 PRD |
| 工程状态 | M2 本地证据解析、哈希复用、定位、状态衔接和测试已完成 |
| 可立即开展 | M3 准则版本加载、条款拆解、统一披露映射和并集完整性检查 |
| 尚未具备 | 正式准则内容、人工审核映射样例、脱敏项目材料、期望输出样例 |
| MVP 目标 | 完成一次从项目创建到母版、矩阵、缺口及准则适配稿的端到端内部试用 |

### 2.1 开发准备度结论

当前内容足够 Codex 开始搭建 Harness 核心和 Codex Skill 适配骨架，但不足以直接完成具备真实领域判断能力的 MVP。

开发可以并行推进两条主线：

1. **工程主线**：搭建框架无关 Harness、项目数据契约、解析器、状态管理和导出框架；
2. **业务主线**：准备准则原文、审核后的条款拆解与映射、脱敏测试材料及人工期望输出。

业务材料未准备完成前，工程主线使用模拟规则和测试夹具，不得自行编造正式准则内容。

## 3. 已确认的关键决策

| 编号 | 决策 |
| --- | --- |
| D-01 | 产品供咨询团队内部使用，不是正式合规认证工具 |
| D-02 | 采用框架无关 Harness 核心 + 轻量 Agent Skill 适配层 |
| D-03 | 顾问指定适用准则；系统只按报告期间推荐版本并等待确认 |
| D-04 | 母版覆盖当前项目所选准则要求的并集 |
| D-05 | 从母版派生准则适配稿，不维护互相失联的多份独立报告 |
| D-06 | 准则回应程度与同业/最佳实践水平分开评价 |
| D-07 | 事实必须有客户证据；推断、建议文本和缺口必须标记 |
| D-08 | 准则映射准确性和证据可追溯性优先于文字精致度和速度 |
| D-09 | 默认生成中文母版，确认后再询问是否生成英文版 |
| D-10 | 项目数据本地优先，每次执行前由用户确认数据处理边界 |
| D-11 | MVP 优先支持 Word、文本型 PDF 和 Excel |
| D-12 | 年度滚动、完整碳核算、重大性分析和复杂协作不进入 MVP |
| D-13 | 借鉴 garden-skills：采用自包含 Skill 包、固定 Checkpoint、阶段 references、确定性 scaffold 和强制自检 |
| D-14 | `disclosure_ledger.jsonl` 是要求、证据、正文和评价关系的唯一真相源，所有交付物由其派生 |
| D-15 | 完整母版前先生成一个代表性章节作为 Anchor，顾问确认后再批量生成其余章节 |
| D-16 | `REQUIREMENTS.md` 的 `AC-01` 至 `AC-12` 是稳定验收基线；新增用例只追加，不复用或重编已有编号 |
| D-17 | M1 采用 Python 3.11+、uv 锁定依赖、PyYAML、pytest 和 Ruff；统一命令由根目录 Makefile 提供 |
| D-18 | M2 支持 `.docx`、文本型 `.pdf` 和 `.xlsx`；扫描版 PDF 必须明确进入 OCR 阻塞状态 |

## 4. 开发前输入清单

### 4.1 阻塞真实领域试用的材料

| 编号 | 材料 | 状态 | 建议负责人 | 最晚时间 | 验收方式 |
| --- | --- | --- | --- | --- | --- |
| IN-01 | 港交所及三大交易所首轮使用的正式准则文件和准确版本 | 待准备 | 准则专家 | 规则库开发前 | 来源、版本、生效日期均可核验 |
| IN-02 | 至少两套准则的人工条款拆解样例 | 待准备 | 准则专家 | 阶段 0 | 每条原文可追溯到可检查要求 |
| IN-03 | 相同、相近和特有要求的人工交叉映射样例 | 待准备 | 准则专家 | 阶段 0 | 映射类型和差异说明已审核 |
| IN-04 | 一套脱敏或模拟客户资料 | 待准备 | 报告团队 | 解析器验收前 | 包含 Word、PDF、Excel |
| IN-05 | 客户任务书或报告规格样例 | 待准备 | 报告团队 | 项目规格功能验收前 | 可提取篇幅、颗粒度和交付要求 |
| IN-06 | 至少一份同行参考报告 | 待准备 | 报告团队 | 同行模块开发前 | 明确风格参考或质量标杆用途 |
| IN-07 | 人工认可的母版粗稿片段 | 待准备 | 报告团队 | 母版生成开发前 | 标注来源证据和对应要求 |
| IN-08 | 人工认可的回应矩阵与缺口清单 | 待准备 | 报告团队、审阅人员 | 评价模块开发前 | 字段和示例结论已确认 |

### 4.2 不阻塞工程骨架、但需按期确认

| 编号 | 决策 | 状态 | 最晚时间 |
| --- | --- | --- | --- |
| IN-09 | 本地运行环境和依赖安装方式 | 已确认：Python 3.11+ 与 uv | 已完成 |
| IN-10 | 允许使用的模型和外部服务 | 待确认 | 首次真实材料试用前 |
| IN-11 | Word 母版和适配版模板 | 待确认 | 导出模块开发前 |
| IN-12 | Excel 矩阵、缺口和证据清单最终样式 | 待确认 | 导出模块开发前 |
| IN-13 | 第二个 Agent 适配对象 | 待确认 | 阶段 5 前 |

## 5. 工作流与责任边界

### WS-0：业务测试基准

**负责人建议**：准则专家、报告团队。

**工作内容**：

- 收集和确认准则版本；
- 制作人工拆解和映射样例；
- 准备脱敏客户材料；
- 制作期望母版、回应矩阵和缺口清单。

**禁止事项**：未经专业人员确认的 AI 拆解不得标记为正式规则。

### WS-1：Harness 基础

**负责人建议**：开发人员或 Codex。

**工作内容**：

- 工程初始化；
- `project.yaml` 模式和校验；
- 项目状态机；
- 公共错误和日志格式；
- JSON/JSONL 数据模型；
- 单元测试和测试夹具框架。

### WS-2：资料解析与证据库

**工作内容**：

- Word、PDF、Excel 解析；
- 文件哈希和增量复用；
- 证据位置、年份、单位及表格元数据；
- 解析失败和扫描 PDF 降级处理。

### WS-3：准则库与统一映射

**工作内容**：

- 准则版本注册和锁定；
- 原始条款、可检查要求和统一披露要求；
- 映射类型、审核状态和变更记录；
- 项目级自定义要求；
- 准则并集完整性检查。

### WS-4：母版、评价和导出

**工作内容**：

- 动态目录；
- 内容块和四类内容状态；
- 母版粗稿；
- 回应矩阵、缺口清单和证据清单；
- 内部版与干净版导出门禁；
- Word 和 Excel 导出。

### WS-5：适配、Agent 和试用

**工作内容**：

- 准则适配稿和差异清单；
- Codex Skill；
- 第二个 Agent 适配层；
- 跨 Agent 连续性测试；
- 试用指标记录和复盘。

## 6. 里程碑

| 里程碑 | 对应工作流 | 交付结果 | 完成门槛 | 状态 |
| --- | --- | --- | --- | --- |
| M0 开发基准就绪 | WS-0 | 测试材料、规则和人工期望输出 | 准则专家与报告团队确认 | 待开始 |
| M1 工程骨架可运行 | WS-1 | 项目初始化、配置、状态机、数据模型和测试 | 可创建项目并恢复状态 | 已完成 |
| M2 证据库可用 | WS-2 | 三类文件解析、定位和复用 | 重复运行不重复解析 | 已完成 |
| M3 多准则并集可用 | WS-3 | 版本锁定、条款拆解和统一映射 | 原始条款无静默丢失 | 待开始 |
| M4 母版与矩阵可审阅 | WS-4 | 母版、矩阵、缺口、证据清单 | 顾问完成一次专业审阅 | 待开始 |
| M5 MVP 试用完成 | WS-5 | 适配稿、双 Agent 接续、试用复盘 | 通过 PRD 验收用例 | 待开始 |

## 7. 首轮开发范围

建议另一个 Codex 对话的首轮任务仅覆盖 `M1 工程骨架可运行`。M1 应优先形成一个 Garden 风格、可复制安装的 Skill Harness，而不是通用后台服务。

### 7.1 首轮必须交付

- 初始化可测试的 Harness 工程；
- 建立 `skills/sustainability-report-harness/` 自包含 Skill 包；
- 创建精简 `SKILL.md`、`manifest.json`、分阶段 `references/` 和确定性 `scripts/`；
- 定义标准项目目录；
- 实现 `project.yaml` 结构和校验；
- 实现 `disclosure_ledger.jsonl` 模式、唯一 ID 和引用完整性校验；
- 实现 PRD 中的项目状态机；
- 定义 `StandardVersion`、`Requirement`、`UnifiedDisclosure`、`Evidence`、`DisclosureContent` 和 `Assessment` 模型；
- 提供统一命令或程序接口完成项目创建、状态读取和配置校验；
- 建立单元测试、示例项目和开发说明；
- 创建模拟准则和示例项目，但不得写入虚构的正式准则知识；
- 建立“生成、校验、修复、再校验”的 QA 协议骨架。

### 7.2 首轮不做

- 不抓取或自行编造准则内容；
- 不实现未经测试材料验证的完整报告生成；
- 不同时适配多个 Agent；
- 不提前建设 SaaS、数据库服务或网页界面；
- 不把模拟规则标记为正式规则。

### 7.3 首轮完成标准

- 新项目可以被创建并产生符合 PRD 的目录；
- 无效配置能返回清晰错误；
- 项目状态可以持久化并恢复；
- Evidence、Outline、Anchor 和 Export Checkpoint 状态可以持久化，且不能绕过；
- 数据模型可以序列化和校验；
- 示例项目和测试全部通过；
- 后续解析器、规则库和生成模块可以通过稳定接口接入。

### 7.4 M1 实际交付记录

**完成日期**：2026-07-10。

**实际交付**：

- 根目录 `pyproject.toml`、`uv.lock`、`Makefile`、`.gitignore` 和用户 `README.md`；
- `skills/sustainability-report-harness/` 自包含 Skill、UI 元数据、版本清单、阶段 references、模板、JSON Schema 和模拟准则；
- 项目脚手架、统一错误与安全日志、六个核心模型、配置校验、账本引用校验、状态机、Checkpoint 持久化和导出预检；
- `examples/m1-demo/` 模拟项目和 `tests/` 自动化测试。

**验证结果**：

- `make install`：通过；
- `make test`：21 项测试通过；
- `make lint`：通过；
- `make format`：通过；
- `make validate`：Skill、模式和示例项目验证通过；
- Skill Creator 官方 `quick_validate.py`：通过。

**已知限制**：

- M1 不解析真实 Word、PDF 或 Excel，不生成报告正文和正式业务交付物；
- 仅包含明确标记的模拟准则，不能用于监管判断；
- 第二个 Agent 的端到端连续性验证留待 M5，M1 只保证公共文件和状态格式可移植；
- 正式领域验收仍依赖 IN-01 至 IN-08 的专业材料。

## 8. 开发过程管理

### 8.1 状态更新规则

每完成一个里程碑，开发对话应更新本文档：

- 将里程碑状态改为“进行中”“已完成”或“阻塞”；
- 记录实际交付文件；
- 记录测试结果；
- 记录新增决策和已知限制；
- 若修改 PRD 行为，必须同步更新 [PRD.md](./PRD.md)。

### 8.2 决策记录格式

新增决策统一追加到“已确认的关键决策”，使用下一个 `D-xx` 编号，并记录日期和原因。不得只把重要决定留在聊天记录中。

### 8.3 阻塞处理

- 缺少业务材料时，使用明确标记的模拟数据继续开发非领域模块；
- 不得用模型猜测代替准则专家确认；
- 阻塞真实验收但不阻塞工程开发时，应分别记录；
- 同一阻塞出现后，应指定负责人和需要的输入。

## 9. 风险与控制

| 风险 | 项目管理措施 |
| --- | --- |
| 范围过大导致迟迟无法试用 | 以 M1-M5 里程碑推进，每次只交付可验收闭环 |
| Skill 承载过多业务逻辑 | Skill 仅做交互和调用，状态及确定性逻辑放入 Harness |
| 没有真实规则就开始调模型 | 先做人工基准，模型输出只作为候选 |
| 多 Agent 同时开发造成接口漂移 | 先稳定公共契约和 Codex Skill，再增加第二个适配层 |
| 只关注生成速度忽视纠错成本 | 试用同时记录总工时、映射纠错和证据错误 |
| 业务决定只存在于对话 | 所有决定更新到 PRD 或本项目计划 |

## 10. 新开发对话交接说明

新的 Codex 开发对话开始时，应先读取：

1. [PROJECT_PLAN.md](./PROJECT_PLAN.md)；
2. [HARNESS_ARCHITECTURE.md](./HARNESS_ARCHITECTURE.md)；
3. [PRD.md](./PRD.md)；
4. [REQUIREMENTS.md](./REQUIREMENTS.md)。

建议首条开发指令：

> 请先阅读项目中的 PROJECT_PLAN.md、HARNESS_ARCHITECTURE.md、PRD.md、REQUIREMENTS.md 和 AGENTS.md。按照 PROJECT_PLAN.md 的“首轮开发范围”完成 M1：参考 garden-skills 的方法，初始化一个自包含、可复制安装的 sustainability-report-harness Skill，落实 SKILL.md、manifest、阶段 references、项目 scaffold、project.yaml、disclosure_ledger 单一真相源、包含硬性 Checkpoint 持久化的状态机、validator、模拟数据和测试。不要编造正式准则内容，不要建设 SaaS 或多个 Agent 适配。完成后运行测试，并更新 PROJECT_PLAN.md 的里程碑状态、交付物、测试结果和已知限制。

## 11. 下一步行动

| 顺序 | 行动 | 负责人建议 | 状态 |
| --- | --- | --- | --- |
| 1 | 完成 M1 工程骨架 | 开发负责人 | 已完成 |
| 2 | 确认首轮测试的两套准则和版本 | 项目负责人、准则专家 | 待开始 |
| 3 | 准备条款拆解和交叉映射人工样例 | 准则专家 | 待开始 |
| 4 | 准备脱敏 Word、PDF、Excel 项目材料 | 报告团队 | 待开始 |
| 5 | 准备期望母版片段、矩阵和缺口清单 | 报告团队、审阅人员 | 待开始 |
